#include <stdio.h>

/**
 * main - display numbers from 0 to 9
 * Return: return 0 and exit the program.
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
